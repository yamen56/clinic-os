# DECISIONS.md

Decisions made autonomously during the build, with one line of reasoning each.

## Infrastructure substitutions

The brief specified Supabase and BullMQ/Redis. Neither was installable here — the machine has no Docker, no Supabase CLI, and no Redis. Each substitution below keeps the capability the brief actually depends on and stays portable.

> **Where it landed.** Kept, and then some: production runs on Railway — the app, the worker and Postgres in one project, with files in Cloudflare R2. Nothing on this list was ever swapped back, and the portability each item was chosen for is what made moving hosts a change of connection string. Supabase appears below only as the thing that was substituted *away from*.

1. **Embedded PostgreSQL instead of hosted Supabase** — runs real PostgreSQL 18 in-process via `embedded-postgres`, so tenant isolation is genuine Row Level Security and realtime is genuine `LISTEN/NOTIFY`. The schema is plain SQL, so pointing `DATABASE_URL` at any hosted Postgres and running `npm run migrate` moves the whole thing over, RLS policies included — which is exactly how it reached Railway.
2. **Custom session auth instead of Supabase Auth** — bcrypt password hashes plus SHA-256-hashed session tokens in Postgres, delivered as httpOnly cookies. Same security properties for this deployment shape, no external dependency.
3. **Local-disk storage adapter instead of Supabase Storage** — files live under `./storage/{clinicId}/…` behind authenticated API routes. `src/lib/storage.ts` is the only module that touches disk, which is what kept the move to S3-compatible storage contained to one file.
4. **SSE over Postgres LISTEN/NOTIFY instead of Supabase Realtime** — database triggers emit `app_events`; one SSE endpoint per clinic fans them out. Reconnects are silent and refetch on resubscribe.
5. **Postgres `jobs` table instead of BullMQ/Redis** — the brief explicitly permits this ("a jobs table if you want fewer moving parts"). Claiming uses `FOR UPDATE SKIP LOCKED` so multiple workers never double-process; `dedupe_key` gives idempotency.
6. **PDF generation via headless Chromium instead of react-pdf** — the invoice *is* a real page (`/inv/{token}`), rendered to PDF by Playwright's Chromium. This was the only approach that gave correct Arabic shaping and RTL table layout without hand-managing font subsets.

## Security and tenancy

7. **One database role (`clinicos_app`, no BYPASSRLS) with an `app.is_admin` escape hatch** — the worker and the auth layer legitimately span clinics. They run in an audited system context; every clinic-scoped request sets `app.clinic_id`, which RLS enforces. `npm run test:rls` asserts isolation on all 29 clinic-scoped tables plus cross-tenant insert and update attempts.
8. **Impersonation issues a separate session carrying `impersonated_by`** — support access is visibly banner-flagged in the workspace, every action is attributable, and exiting destroys that session rather than just navigating away.

## Product

9. **Path-based public booking (`/book/{slug}`) rather than a subdomain** — no DNS control in local dev; the route maps 1:1 to `book.domain.com/{slug}` behind a production rewrite.
10. **Platform accent in the staff workspace, clinic branding on patient-facing surfaces** — the booking page and invoice PDFs carry the clinic's logo and colour; the staff UI stays visually consistent so agency support isn't relearning a new skin per clinic.
11. **Ambiguous `05…` local numbers resolve to Saudi (+966) unless the clinic's country is UAE** — SA and AE share the local mobile shape; SA is the larger Gulf market, and the clinic's own country breaks the tie.
12. **Doctors don't see Conversations, Invoices, Automations, or Settings** — matches the role spec. Receptionists see everything except automations and AI unless the owner grants it via a per-member permission flag.
13. **Arabic UI renders Latin digits (`ar-JO-u-nu-latn`)** — standard in Jordanian and Gulf medical software; keeps phone numbers, prices, and times unambiguous.
14. **WhatsApp offline doesn't block online booking** — if the clinic's WhatsApp is down, the booking page skips phone verification, books anyway, and flags the appointment as unverified rather than losing the patient.
15. **The AI receptionist defaults to after-hours only** — staff cover the day; the agent covers nights and weekends. It's the safest default for a clinic switching it on for the first time, and it's one dropdown to change.
16. **AI answers strictly from the clinic's knowledge base plus live calendar** — no general knowledge, no invented prices. Anything it wasn't taught becomes a staff handoff. Every AI message is labelled in the inbox for auditing.
17. **AI runs at `effort: "low"`** — a receptionist turn is short and scoped (answer from a fixed knowledge base, check availability, book), and WhatsApp replies are latency-sensitive. The model is per-clinic configurable if a clinic wants more.
18. **Automation idempotency is scoped to (automation, patient, appointment, invoice)** — not to the run. A replayed trigger creates a new run, so run-scoped deduplication would have let the same reminder go out twice; this scope survives both retries and replays.
19. **Critical alerts fall back to WhatsApp when push fails** — WhatsApp disconnection, repeated send errors, and AI escalations reach staff on their own number if no push subscription is live, since those are exactly the failures that make the app itself untrustworthy.

## Build and tooling

20. **TypeScript pinned to 5.x** — npm resolved `typescript@7` (native preview), which Next 15's tooling doesn't support yet.
21. **Baileys pinned to 6.7.21** — 7.0.0-rc13 pulls `whatsapp-rust-bridge`, whose package exports break Node's resolver on this platform.
22. **PostgreSQL cluster initialised with `--encoding=UTF8 --locale=C`** — Windows `initdb` defaults to WIN1252, which cannot store Arabic at all. This was caught by the first end-to-end test rather than in review.
23. **The AI receptionist is QA'd against a local Messages API double** — no `ANTHROPIC_API_KEY` is available in this environment. The double exercises the agent's real code path end to end (tool calls, availability lookup, booking through the identity rule, escalation, usage accounting); only Anthropic's model is stubbed. Set the key to run it live.
24. **Web push is QA'd against a local TLS endpoint** — encryption, delivery, and pruning of revoked subscriptions are all verified for real. Headless browsers can't reach a live push service, so `pushManager.subscribe()` itself is only exercised as far as the permission grant.

## Document signing (ESIGN module)

25. **`patient_field_definitions` replaces `custom_field_defs` outright** — the brief needs one list of fields driving the patient form, the merge-variable picker and the document preview. Two tables would have meant two truths, so migration 0010 copies the old rows across and nothing reads the old table any more. Context values the clinic does not store per patient (clinic name, booked service, price, today's date) live in the same table under `scope = 'context'`, which is what lets a clinic rename or hide *any* variable, as specified.
26. **`document_events` is append-only by trigger, but a cascade may still remove it** — refusing every DELETE also refused the cascade, which made a patient (and therefore a clinic) undeletable and broke every existing QA suite's teardown. The guarantee that matters is that *while a document exists* its history cannot be edited or trimmed: UPDATE is refused always, DELETE only when the parent document is already gone, which is true solely inside a cascade. Migration 0013.
27. **The frozen snapshot, not the PDF, is the authoritative record** — Chromium shapes Arabic correctly, and that is exactly why the glyphs it embeds are presentation forms in visual order: the text layer of any browser-printed Arabic PDF is lossy and cannot be searched or reliably extracted. `tagged: true` makes no difference and NFKC folding recovers the letters but not their order. So `documents.content_snapshot` is what the SHA-256 covers and what any dispute is resolved against; the PDF is the human-readable artifact, and it carries its title, id and fingerprint as PDF metadata so an archive of thousands stays searchable by the fields that matter.
28. **Headless Chromium for template documents, pdf-lib only as a compositor for uploads** — as the brief requires. An uploaded PDF is never re-typeset: transparent per-page overlays are rendered by the same browser (so Arabic values shape correctly) and composited onto the untouched original, then the certificate is appended. That is why the worker gained `/render-overlays`.
29. **The kiosk has no hand-over gate** — an earlier "hand the device to X, tap Start" screen cost the patient a tap the brief did not budget, and the wrong one: staff are meant to tap "Sign on this device" and hand over a device already showing the document. The instruction is now a strip staff read over the patient's shoulder; containment arms immediately. Measured at 4 staff taps and 3 patient taps.
30. **Signing screens take their language from the document, not the visitor** — the signature pad originally read the workspace i18n provider, which put Arabic buttons on an English consent form for any patient with no language cookie, i.e. every first-time visitor. `SignaturePad` now accepts an explicit `locale`; `useI18nSafe` exists so the same component still works inside the workspace provider.
31. **A document raised from a patient's file adopts their next appointment** — without it the doctor, service and price merge empty and sending is blocked, which reads as a bug rather than as missing data. The resolved appointment is recorded on the document so the merged values keep a traceable source.
32. **The two patient reminders are built in; the escalations are recipes** — 24h and 72h reminders are driven by the clinic's own "first reminder after" setting so they are hour-accurate and cannot be switched off by accident, and duplicating them as automations would send every patient the same nudge twice. What ships as editable, default-on recipes is what a flow can do that a fixed reminder cannot: escalate an unsigned form to a phone-call task at day five, chase an expiry, open a task on a decline. A service's required consent form is raised by a direct hook on appointment confirmation rather than a recipe, because which form is needed depends on the booked service — one recipe could only ever name one template.
33. **`execCommand` for the template body editor** — formally deprecated and still the only way to get real rich text with no dependency, and a consent form needs exactly what it does well. Everything it emits passes through the server-side allowlist sanitiser before storage, so the browser's output is never trusted.
34. **Placed field coordinates are stored as page fractions** — a box placed on a phone lands in the same spot when the page prints at A4, and survives a different zoom next week.
35. **Editing a template publishes a new version rather than mutating one** — documents already sent read their own frozen snapshot, so the version is for the record, not for correctness; `document_template_versions` means a completed document can always be traced to the exact wording it came from.
36. **`jsonb` read back from pg is re-serialised before being written again** — node-pg encodes a JavaScript *array* as a Postgres array literal, so passing a `fields_schema` straight through produced `{}` and failed with "invalid input syntax for type json", taking the whole clinic-creation transaction down. Caught by the phase 1 suite, not by review.
37. **"Send all pending" suppresses each document's own delivery** — the first version called the normal send path per document (which messages the patient) and *then* sent the bundle, so a patient with two forms received three messages. `sendDocument` gained `suppressDelivery`, and the bundling moved into `sendAllPendingForPatient` in `flow.ts` so the QA suite exercises the real function rather than a copy of it. Each document still gets its own single-use link; only the message is combined.
38. **Two pre-existing suites were made date-independent** — phase 4 took the second chip in the booking date strip ("tomorrow") and phase 8 asked the AI to book "بكرا", both of which fail one day in seven because the seeded clinic closes on Friday. Phase 4 now picks the first day that actually offers slots (and reuses that day for its second booking, so "the taken slot disappeared" stays a real comparison); phase 8's fixture clinic opens every day, since what it tests is the agent's tool loop, not the hours.

## Access

39. **A job title and an access set are two columns, not one** — `clinic_members.role` used to carry both, which is why every gate in the app was spelled `role <> 'doctor'` or `role = 'owner'`. That made the two most ordinary requests a clinic has unrepresentable: let this doctor see the inbox, stop that receptionist touching invoices. Migration 0014 splits them. `role` is the job (`doctor` | `receptionist` | `other`) and stays load-bearing for scheduling — only a doctor is bookable and has working hours. `is_owner` is a flag, because ownership was never a job: the owner is also a doctor or a receptionist. Access lives in `permissions` as `{level:'full'}` or `{level:'custom',caps:{…}}`, and every gate now reads `can(access, cap)`.
40. **"Full access" is a level, not every box ticked** — the two would behave identically today and diverge on the next release: a member on `full` picks up new capabilities automatically, while a hand-ticked set silently keeps a new screen hidden until somebody notices. Owners get `full` unconditionally, whatever is stored, so a clinic cannot lock itself out of its own account.
41. **Capability dependencies are enforced in the resolver, not only in the UI** — a stored map that grants `documents.void` without `documents` would otherwise produce a member who can cancel a signed agreement they cannot open. `resolveCapabilities` clears dependent actions whose section is off, so a hand-edited row denies rather than grants, and the server action rebuilds the map from the known capability list rather than storing what the client sent.
42. **A delegated staff manager cannot edit an owner or themselves** — `settings.staff` is grantable, which means it can be given to someone who should not be able to deactivate the owner or widen their own access. Both rules live in the server action; the screen only explains them.
43. **An uploaded signed copy is marked as one and never dressed up as a captured signature** — clinics do print, sign and scan, and before this the record simply stayed "sent" forever. But nothing about a scan is verifiable here: no captured signature, no IP, no one-time code, no content hash. So `documents.final_pdf_source` records the provenance, the signers the uploader ticks are recorded as having signed on paper, the audit trail names who attached it, and the detail screen says all of this at the top — because the completed badge, the timestamps and the download button look identical either way.
44. **Word imports; PDF only gives up its text** — `.docx` is structured XML, so the conversion keeps headings, lists and emphasis and the result is genuinely editable. A PDF stores positioned glyphs with no notion of a paragraph, and for Arabic the text layer is lossy in the same way our own output is (decision 27) — extraction can come back reversed and disconnected. Both are offered, the import is previewed before it is accepted, nothing is written until then, and the PDF path warns about exactly what it lost. Keeping the file as-is remains the better option and is one button away.
45. **The document list loads both scopes and filters in the browser** — the tabs used to push `?scope=` and wait for a server render, which put a full round trip between the press and any visible change. Two capped selects cost one extra query on load; the QA suite asserts the switch fires no navigation and lands under 400ms.

## Booking intake

46. **Booking answers are frozen onto the appointment, not joined to the question** — `appointments.intake_answers` stores `{id, label, labelAr, type, value}` per answer rather than a foreign key into `booking_questions`. Clinics reword their questions constantly and delete the ones that stopped earning their place; a join would silently rewrite what a patient was asked three months ago, and deleting a question would blank the answer entirely. The snapshot costs a little duplication and buys a record that stays true. It is also why deleting a question is a safe, unguarded action.

47. **A booking question maps onto an existing patient field rather than defining its own storage** — `booking_questions.patient_field_key` names a `patient_field_definitions` row, so "where do allergies live" has exactly one answer across the profile form, the document merge variables and the booking page. The alternative — a second little schema owned by booking — would have made a clinic define the same field twice and then watch the two disagree.

48. **A booking form fills blanks and never overwrites** — every write in `applyAnswersToPatient` is a `coalesce`. A returning patient's birth date was checked against an ID card at the desk; whatever gets typed on a phone in a waiting room is not better evidence than that. Name and phone are not writable from a booking form at all, phone being the identity rule, and only three columns are exposed to the mapping — everything else lands in `custom_fields`.

49. **Answers are validated server-side against the clinic's rows, not against the submitted form** — the booking page is the one surface where the form itself is attacker-controlled, so `/start` re-derives which questions apply to the chosen service and re-checks required, type and option membership. A question the browser chose not to render is still required.

50. **The date strip is told what is open rather than discovering it one tap at a time** — `computeDayCounts` loads the service, the candidate doctors and the busy scan once for the whole window and then walks the days in memory, so `/days` answers for thirty days in one round trip and roughly the cost of one. Doing it as thirty `computeSlots` calls would have been ninety queries serialised on a single pg connection. Closed and full days are then greyed out, the wizard opens on the first day that has something, and an empty day offers the next one — which is most of what made booking on a clinic that opens three days a week feel broken.

51. **`Field` lost its `htmlFor`** — it carried a `useId()` that was never applied to the control inside, and a `for` naming nothing is worse than no `for` at all: the browser stops looking, so every field in the app had no labelled control. Clicking a label focused nothing and assistive tech read the input as unlabelled. Removing the attribute restores the implicit association a wrapping `<label>` already had. Found by the booking UI suite, which could not type into a field by its label.
